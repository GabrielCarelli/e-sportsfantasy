import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['upload.wikimedia.org', 'liquipedia.net', 'static.wikia.nocookie.net', 'encrypted-tbn0.gstatic.com', "www.pngitem.com"],
  },
};

export default nextConfig;
